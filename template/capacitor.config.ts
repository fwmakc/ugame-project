import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.ugame-project',
  appName: 'ugame-project',
  webDir: 'dist'
};

export default config;
